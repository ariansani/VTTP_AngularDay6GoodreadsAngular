export interface BookSummary {
    bookId: string
    title: string
}

export interface BookDetail{
    bookId:string
    title:string
    author:string
    description: string
    pages: number
    rating: number
    image:string
}